<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>订单管理后台</title>
<style>
body{background:#f7f7f7;font-family:微软雅黑}
.container{max-width:900px;margin:0 auto;padding:1rem}
.order{background:white;padding:1rem;border-radius:12px;margin:1rem 0}
h2{color:#d92121}
</style>
</head>
<body>
<div class="container">
  <h2>🛎️ 订单管理后台</h2>
  <div id="orderList"></div>
</div>
<script>
const orders = JSON.parse(localStorage.getItem("orders"))||[];
const el = document.getElementById("orderList");
orders.forEach(o=>{
  el.innerHTML += `
  <div class="order">
    <div><b>订单：</b>${o.id}</div>
    <div><b>客户：</b>${o.name} ${o.phone}</div>
    <div><b>地址：</b>${o.address}</div>
    <div><b>时间：</b>${o.time}</div>
    <div><b>金额：</b>¥${o.total}</div>
  </div>`;
});
</script>
</body>
</html>