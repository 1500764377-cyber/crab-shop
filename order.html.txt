<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1.0">
<title>我的订单</title>
<style>
body{background:#f7f7f7;font-family:微软雅黑}
.container{max-width:800px;margin:0 auto;padding:1rem}
.order{background:white;padding:1rem;border-radius:12px;margin:1rem 0}
.title{font-weight:bold}
.money{color:#d92121;font-weight:bold}
</style>
</head>
<body>
<div class="container">
  <h2>我的订单</h2>
  <div id="list"></div>
</div>
<script>
const orders = JSON.parse(localStorage.getItem("orders"))||[];
const list = document.getElementById("list");
orders.forEach(o=>{
  list.innerHTML += `
  <div class="order">
    <div class="title">订单号：${o.id}</div>
    <div>客户：${o.name} ${o.phone}</div>
    <div>地址：${o.address}</div>
    <div>时间：${o.time}</div>
    <div class="money">金额：¥${o.total}</div>
  </div>`;
});
</script>
</body>
</html>